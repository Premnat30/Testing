<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Search Results</title>
    <style>
        body { font-family: sans-serif; }
        .container { max-width: 800px; margin: auto; padding: 20px; }
        pre { background-color: #f4f4f4; padding: 10px; border: 1px solid #ddd; white-space: pre-wrap; word-wrap: break-word; }
        /* New styles for highlighting */
        .error-highlight {
            color: red;
            font-weight: bold; /* Optional: Make it bold for emphasis */
        }
        .warning-highlight {
            color: orange; /* Using orange for better visibility against #f4f4f4 background */
            font-weight: bold; /* Optional: Make it bold for emphasis */
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Search Results</h1>
        <p>Showing results for `{{ query }}` in `{{ selected_log }}`.</p>
        <a href="/?hostname={{ hostname_key }}" class="back-link">Back to file list</a>
        {% if results %}
            <p>Found {{ results|length }} matching lines:</p>
            <pre>
                {% for line in results %}
                    {{ line | replace('ERROR', '<span class="error-highlight">ERROR</span>') | replace('Warning', '<span class="warning-highlight">Warning</span>') | safe }}
                {% endfor %}
            </pre>
        {% else %}
            <p>No matching logs found.</p>
        {% endif %}
    </div>
    
   
</body>
</html>
